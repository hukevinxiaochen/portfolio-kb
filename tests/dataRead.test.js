const test = require("tape");

/**
 * test JSON2Neo4j
 *
 * write functions that read a custom JSON file, interpret the nodes
 *  and edges, and run appropriate database actions to write these to
 *  a specified Neo4j database.
 */
